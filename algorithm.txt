program pascal;
uses sysUtils , crt ;

declaration variables
pattern ,option : Char;
i , j , rows , cols , spaces : integer;

// declaration functions
procedure rightAngledTriangle(option : type = Char): returned value = integer;
    start 
     if option <- '1' then
          print('Enter the number of rows : '); ReadLn(rows);
        // stars
        i <- 0;
        while i <= rows - 1 do
          begin
            j <- 0;
            while j <= i do          
                print('*'); 
                Inc(j);
              WriteLn; // to write a new line
              Inc(i);
          end;
      end
      else if option <- '2' then
      begin
          print('Enter the number of rows : '); ReadLn(rows);
          for i := 0 to rows - 1 do 
                begin
                  for j := 0 to i do         
                    Write(j:3);
                  WriteLn; // to write a new line
                end;
      end
      else
      begin
          print('Invalid option');
      end;
      rightAngledTriangle := 1;
  end of function

//  diamond
 procedure diamond(option: type = Char):returned value = Integer;
    begin
      if option = '1' then
        begin
          print('Enter the number of rows : '); ReadLn(rows);
            // print upper half of diamond
          i := 1;
          while i <= rows do
            begin
             // print spaces
            spaces := rows - i;
              j := 1;
              while j <= spaces do
                begin
                  Write(' '); 
                  inc(j);
                end;

                // print the stars
                j := 1;
                asterisk := 2 * i - 1;
                while j <= asterisk do
                  begin
                    Write('*');
                    Inc(j);
                  end;
                  writeln;
                  Inc(i);
            end;
             // print lower half of diamond
            i := rows - 1;
            while  i >= 1 do 
              begin
                j := 1;
                spaces := rows - i;
                while j <= spaces  do
                  begin
                    Write(' ');
                    j := j + 1;
                  end;
                  // print stars
                  j := 1;
                  asterisk := 2 * i - 1;
                  while j <= asterisk do
                    begin
                      Write('*');
                      j := j + 1;
                    end;
                  writeln;
                  i := i - 1;
              end;
            end
            else if option = '2' then
              begin
                // print upper half of the diamond
                print('Enter number of rows: '); Readln(rows);
                i := 1;
                while i <= rows do
                  begin
                    j := 1;
                    spaces := rows - i;
                    while j <= spaces do
                      begin
                        Write(' '); 
                        Inc(j);
                      end;
                      // print the number
                      j := 1;
                      asterisk := 2 * i - 1;
                      while j <= asterisk do
                        begin
                          Write(j);
                          Inc(j);
                        end;
                        writeln;
                        Inc(i);
                  end;
                  // lower half of diamond
                  i := rows - 1;
                  while i >= 1 do
                    begin
                      j := 1;
                      spaces := rows - i;
                      while j <= spaces do
                        begin
                          Write(' ');
                          Inc(j);
                        end;
                        // print the number
                        j := 1;
                        asterisk := 2 * i - 1;
                        while j <= asterisk do
                          begin
                            Write(j);
                            Inc(j);
                          end;
                          writeln; // write a new line
                          i := i - 1;
                    end;
              end
              else
              begin
                print('You can only choose 1 or 2');
              end;
              diamond := 1;
    end;

    procedure pyramid(option : type = Char):returned value = Integer;
      begin
        if option = '1' then
        begin
          print('Enter the number of rows : '); read rows;
          for i := 1 to rows do
            begin
              spaces := rows - i;
              for j := 1 to spaces do
                Write(' ');

                //  print stars
                asterisk := 2 * i - 1;
                for j := 1 to asterisk do
                  print('*');
                  WriteLn;
            end;
            end
            else if option = '2' then
              begin
                print('Enter the number of rows : '); read(rows);
                for i := 1 to rows do
                  begin
                    spaces := rows - i;
                  for j := 1 to spaces do
                  print(' ');

                     // print number 
                     asterisk := 2 * i - 1;
                    for j:= 1 to asterisk do
                      print(j);
                      WriteLn;
                  end;
              end
              else 
              print('You can only choose 1 or 2');
              pyramid := 1;
      end;

       procedure upSideDownPyramid( option : type = Char):returned value =  Integer;
        begin
          if option = '1' then
          begin
          print('Enter the number of rows: ');
          read(rows);

          i := rows - 1;
          while  i >= 1 do
            begin
              j := 1;
              spaces := rows - i;
              while j <= spaces do
                begin
                  print(' ');
                  Inc(j);
                end;

                j := 1;
                asterisk := 2 * i - 1;
                while j <= asterisk do
                  begin
                    print('*');
                    inc(j);
                  end;
                  WriteLn;
                  i := i - 1;
            end;
          end
          else if option = '2' then
            begin
              print('Enter the number of rows :');
              read(rows);

              i := rows - 1;
              while i >= 1 do
                begin
                  j := 1;
                  spaces := rows - i;
                  while j <= spaces do
                    begin
                      print(' ');
                      inc(j);
                    end;

                    j := 1;
                    asterisk := 2 * i - 1;
                    while j <= asterisk do
                      begin
                        print(j);
                        inc(j);
                      end;
                      WriteLn;
                      i := i - 1;
                end;
            end
            else
            print('You can only choose 1 or 2');

            upSideDownPyramid := 1;
        end;

         procedure rectangle(option :type = Char):returned value = Integer;
          begin
            if option = '1' then
            begin
              print('Enter the number of rows : '); read(rows);
              print('Enter the number of columns : '); read(cols);

              i := 1;
              while i < rows do
                begin
                  j := 1;
                  while  j < cols do
                    begin
                      Write('*');
                      Inc(j);
                    end;
                    writeln;
                    Inc(i);
                end;
            end
            else if option = '2' then
              begin
                 print('Enter the number of rows : '); read(rows);
              print('Enter the number of columns : '); read(cols);

              for i := 1 to rows do
                begin
                  for j := 1 to cols do
                    print(j:2);
                  WriteLn;
                end;
              end
              else
              print('U can only choose 1  or 2');
              rectangle := 1;
          end;

          end of functions declaration
begin the program
repeat
	print(' -----------------Welcome to my program---------------- ')
	print('This program provides you the pattern that contains stars or numbers')
	print('Choose the pattern you want : ');
 	print('1. Right-angled triangle');
  	print('2. Diamond');
  	print('3. Pyramid');
  	print('4. Rhombus');
  	print('5. Rectangle');
  	read(option);

 case option of
	1 : 
	start case 1
	    print('Choose the stars or numbers : '); 
	    print('1. Stars');
        print('2. Numbers ');
	    read(option);

        // call the function 
	    rightAngledTriangle(option);
	end case 1

    2 : // diamond
    start 
        print('Choose number or stars : ');
        print('1. Stars');
        print('2. Numbers');
        read option;

        diamond(option);
        end of option 2

        3 : // pyramid
            start   
                print(' choose stars or numbers ');
                print('1. Stars');
                print('2. Numbers');
                read option;

              pyramid(option);
              end of option 3

        4 : // upSideDownPyramid
            start
                print(' choose stars or numbers ');
                print('1. Stars');
                print('2. Numbers');
                read option;

                // print the Rhombus
                print (' Enter the number of rows ' );
                read rows;

                upSideDownPyramid(option);

            end;
        end of option 4;

        5 : // rectangle
        start
            print(' choose stars or numbers ');
            print('1. Stars');
            print('2. Numbers');
            read option;
        
         
            rectangle(option);
        end
        else 
            print ( invalid option , u can only choose 1 - 5 ) ;

        print (' Regenrate the pattern') ;
        read generate

        until generate = 'no' ;
    end.
    end of program

	    
  		